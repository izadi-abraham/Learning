

curl -s -o dev/null -w "%{http_code} - %{time_total}s" --max-time 30 https://list-api.fly.dev/health 2>&1

-s silent mode
-o output => -o dev/null discards the response body
-w write out specific variables

---------------------------------------------------------

flyctl status 2>&1 || echo 'flyctl not available or not logged in'

-----------------------------------------------------------

lsof -iTCP -sTCP:LISTEN -n
